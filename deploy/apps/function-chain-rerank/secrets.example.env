# Key names only. Provide the values to SRE through an approved offline channel;
# SRE creates the milvus3-minio-secret Secret in the demo namespace manually.
# The Function Chain demo's MinIO client reads these from MINIO_ACCESS_KEY /
# MINIO_SECRET_KEY (see deployment.yaml).
access_key=<required>
secret_key=<required>
